---
---

<div class="gate" id="gate" data-state="idle">
    <div class="content-wrapper">
        <h1 id="title" class="title">Reaction Time Test</h1>
        <p id="instructions" class="instructions">Click as quickly as you can once red turns green.</p>
        <p class="action-text" id="action-text">Click to start</p>
    </div>
</div>

<style>
    .gate {
        width: 100vw;
        height: 100vh;
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        cursor: pointer; 
        user-select: none;
        padding: 2rem;
    }


    .gate[data-state="idle"] { background-color: var(--color-blue);}
    .gate[data-state="waiting"] { background-color: var(--color-red); cursor: cell; }
    .gate[data-state="go"] { background-color: var(--color-green); }
    .gate[data-state="too-soon"] { background: var(--color-blue); }
    .gate[data-state="result"] { background-color: var(--color-blue); }


    .content-wrapper {
        display: flex;
        flex-direction: column;
        align-items: center;
        text-align: center;
        max-width: 680px;
        width: 100%;
    }

    .title {
        font-size: 5rem;
        font-weight: 800;
        marigin: 0 0 1rem 0;
        line-height: 1.1;
        letter-spacing: -0.03em;
    }

    .instructions {
        font-size: 1.5rem;
        margin: 0 0 2rem 0;
        font-weight: 400;
    }

    @media (max-width: 768px) {
        .title {
            font-size: 3.5rem;
        }
        .instructions {
            font-size: 1.25rem;
        }
    }


</style>

<script>
import { actions } from "astro:actions";
import { _ZodString } from "astro:schema";

    const gate = document.getElementById("gate")!;
    const title = document.getElementById("title")!;
    const instructions = document.getElementById("instructions")!;
    const actionText = document.getElementById("action-text")!;

    let state = "idle";
    let timeoutId;
    let goTimestamp;

    function setState(next: string) {
        state = next;
        gate.dataset.state = next;
    }

    function startRound() {
        setState("waiting");
        title.textContent = "Wait for green...";
        instructions.textContent = '';
        actionText.textContent = '';
        const delay = 1000 + Math.random() * 3000;

        timeoutId = setTimeout(() => {
            goTimestamp = performance.now();
            setState("go");
            title.textContent = "Now!";
        }, delay)
    }

    document.addEventListener("keydown", (e) => {
        if (e.code !== "Space") return;
         

        e.preventDefault();
        if (state == "idle" || state === "result" || state === "too-soon") {
            startRound();
        } else if (state === "waiting") {
            clearTimeout(timeoutId!);
            setState('too-soon');
            title.textContent = 'A bit too soon spammer!';
            instructions.textContent = 'Hit space to try again';
        } else if (state === "go") {
            const reaction = Math.round(performance.now() - goTimestamp!);
            setState("result");
            title.textContent = `${reaction} ms`;
            instructions.textContent = "Hit space to try your skills again!";
            
        }
    });

</script>
